# git init  -initialize a empty repo
# git clone <repo >  -copy from remote
# git branch < branch name >  -create new branch
# git branch -a  - show all branches
# git branch   - shows which branch you are in
# git branch -m <old branch name> <new branch name>
# git checkout <branch name> - changes branches
# git add -a   - adding all changes to staging area
# git commit -m 'comment'  - saving all added changes to staging area in your local
# git push -u origin <branch name> 
# git pull  #pull files from remote to local
# git merge  <branch to be merged with current branch>

#commit or add first ? 